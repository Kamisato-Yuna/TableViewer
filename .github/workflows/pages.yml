name: Product website

on:
  push:
    branches: [main]
    paths:
      - "site/**"
      - "script/build_site.py"
      - "docs/screenshots/**"
      - "TableViewer/Services/ConnectionVault.swift"
      - ".github/workflows/pages.yml"
  release:
    types: [published, edited, deleted]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: github-pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
      - uses: actions/configure-pages@v5
      - name: Read the latest stable release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh api repos/${{ github.repository }}/releases/latest > "$RUNNER_TEMP/release.json"
      - name: Build the product website
        run: python3 script/build_site.py --release-json "$RUNNER_TEMP/release.json"
      - uses: actions/upload-pages-artifact@v4
        with:
          path: _site
  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
