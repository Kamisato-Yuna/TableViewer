# 0.4.0 开发与验收

状态：开发中，尚未发布。工作分支 `codex/0.4.0-workbench`，起点 `06c45f2`。

范围：完整覆盖本次 A–E 需求。继续使用原生 SwiftUI/AppKit、现有驱动、沙箱、Keychain、更新签名与公证流程；不添加新的 hash、baseline、contract 冻结或发布 gate。私人反馈与测试记录不进入公开仓库。

| 需求 | Issue | 验收重点 |
| --- | --- | --- |
| 撤销重做 | #7 | 原生 UndoManager、输入/粘贴/注释、脚本切换 |
| 结构、约束过滤、索引与 DDL | #8 | SQLite/PostgreSQL 实际结构及导出 |
| 选区运行 | #9 | 按钮/快捷键一致、Unicode 选区 |
| 多选、单元格编辑、删除、属性栏偏好 | #10 | Shift/Command/键盘、确认删除 |
| 空字符串与 NULL | #11 | 预览、保存与导出不混淆 |
| 可拖动上下/左右布局 | #12 | 分隔、尺寸、焦点及浅深色 |
| 概览/分类/关系 | 后续编号见里程碑 | 不自动打开首表、真实外键 |
| 多语句/多结果/错误/预估 | 后续编号见里程碑 | 方言边界、失败停止、事务与未知估算 |
| 编辑器排版/行号/快捷键 | 后续编号见里程碑 | 克制高亮、整行操作、注释 |
| 本地多脚本 | 后续编号见里程碑 | 命名、恢复、最近10个、确认清理 |
| Agent | 后续编号见里程碑 | 空配置引导、三级审批、执行可见、隔离 |
| 筛选/导出/只读 | 后续编号见里程碑 | 条件确认、六类格式、引擎写入阻止 |

多语句按顺序执行，首次失败停止并保留先前结果。没有隐式批次事务；用户的 BEGIN/COMMIT/ROLLBACK 仍由数据库解释。自动提交的先前写入不会因为后续失败被撤回。失败后的显式事务状态须告知用户。

验证依次记录源代码/自动测试、各真实引擎、真实 .app 交互、最终签名分发物、线上 Release/更新/Pages。任何层级不得替代另一层级。只使用合成数据库与隔离脚本/Agent 设置，原应用数据和凭据保留。
