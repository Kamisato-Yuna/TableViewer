# Agent 导航与最近会话

2026-09-08。

- 左侧移除 Agent 栏目，顶栏 AI 按钮直接进入当前数据库 Agent。
- 优先恢复未归档的生成中／执行中会话，其次当前选中的本库会话，再选本库最近会话；没有可用会话才新建。
- 打开会话只保存选择，不更新历史排序；只有新用户消息更新时间，当前数据库最近 10 个未归档会话在可折叠右侧栏显示，保留所有会话入口。
- 执行审批选择器移至发送按钮上方，运行中和待审批时的禁用规则不变，结果发送确认文案和流程保留。
- 对象概览增加“让 Agent”与三个示例按钮，创建当前数据库的新会话并填入草稿，不调用 API。

验证命令：

```sh
./script/test_workbench040.sh --agent-navigation-only
python3 script/check_localizations.py
./script/build_and_run.sh --build
```

七项定向测试通过：空历史创建、恢复且保留草稿、数据库隔离、运行中优先、归档排除、示例仅创建草稿、打开选择持久化。
Debug 构建通过，中英文 616 项校验通过。

本机 Studio 原生界面验证：左栏无 Agent 栏目，概览示例可见，顶栏进入 Agent，右栏展开并切换最近会话；截图确认审批选择器在发送按钮上方。点击“帮我了解当前数据库”后输入框填入草稿，未点击发送、未改变审批模式。数据库/API 操作执行不属于本轮回归。
